# Example React

## Project setup

### Node

The project requires node installed and the commands `node`, `npm` and `npx` available in the PATH.

### Prepare frontend

Go to `src/main/react` and run `npm install`.

### Build project

Go to the root folder and run `./gradlew build`.
